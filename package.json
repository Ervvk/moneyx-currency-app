{
  "name": "moneyconverter",
  "version": "1.0.0",
  "description": "",
  "main": "snippets.js",
  "dependencies": {
    "currency-flags": "^3.2.0",
    "node-sass": "^5.0.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.2.5",
    "npm-run-all": "^4.1.5",
    "postcss-cli": "^8.3.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch:sass": "node-sass sass/main.scss css/style.css -w",
    "live-run": "live-server",
    "start": "npm-run-all --parallel live-run watch:sass ",
    "prefix:css": "postcss --use autoprefixer -b \"last 10 version\" css/style.css -o css/style.prefix.css"
  },
  "author": "ervk",
  "license": "ISC"
}
